# Core data stack
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=12.0          # fast parquet I/O

# Modeling
scikit-learn>=1.3
lightgbm>=4.0

# Explainability (Speed & Explainability judging axes)
shap>=0.44

# Config & CLI
pyyaml>=6.0

# Visualization (EDA)
matplotlib>=3.7
seaborn>=0.12

# Kaggle data download
kaggle>=1.6

# Optional — semantic embedding features (CPU-friendly multilingual model).
# Uncomment to enable embedding-based similarity features:
# sentence-transformers>=2.2
